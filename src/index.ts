import "dotenv/config";
// import { runCrawler } from "./crawler";
// import { StrapiClient } from "./strapi.client";

async function main() {
//   const scheduleUrl = process.env.SCHEDULE_URL!;
//   const strapi = new StrapiClient(process.env.STRAPI_URL!, process.env.STRAPI_TOKEN!);

//   await runCrawler(scheduleUrl, async (event) => {
//     await strapi.upsertEvent(event);
//     console.log("Saved:", event.name, event.startDate);
//   });

}

// main().catch((e) => { console.error(e); process.exit(1); });